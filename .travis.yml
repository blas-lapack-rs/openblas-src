language: rust
sudo: false

os:
  - linux
  - osx

rust:
  - stable

before_install:
  - curl https://stainless-steel.github.io/travis/gfortran.sh | bash

addons:
  apt:
    packages:
      - gfortran

notifications:
  email: false
